import keras
import cv2
import numpy as np

from keras.models import load_model
from keras.backend import image_data_format

def process_neumes(image, coords, model):

    labels = ['1', '2', '3']



    return labels
